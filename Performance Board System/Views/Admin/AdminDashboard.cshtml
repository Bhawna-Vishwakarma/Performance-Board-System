@model IEnumerable<Department>
@{
    ViewData["Title"] = "Admin Dashboard ";
    var userName = Context.Session.GetString("UserFullName");
    var userEmail = Context.Session.GetString("UserEmail");
    var userRole = Context.Session.GetString("UserRole");
    // int count = 1;
}


<div class="dashboardLayout" id="dashboardLayout">
    <!-- Main Content -->
    <div class="mainContent p-3" id="mainContent">
        <h3>Welcome @userName 👋</h3>
        @* <h3>Department List</h3>
        <a asp-action="CreateDepartment" class="btn btn-primary mb-2">Add New</a>
        <table class="table table-bordered">
            <thead>
                <tr><th>ID</th><th>Name</th><th>Actions</th></tr>
            </thead>
            <tbody>
                @foreach (var dept in Model)
                {
                    <tr>
                        <td>@count</td>
                        <td>@dept.DepartmentName</td>
                        <td>
                            <a asp-action="EditDepartment" asp-route-id="@dept.DepartmentID" class="btn btn-sm btn-warning">Edit</a>
                            <form asp-action="DeleteDepartment" asp-route-id="@dept.DepartmentID" method="post" style="display:inline">
                                <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm(`Are you sure want to Delete @dept.DepartmentName?`)">Delete</button>
                            </form>
                        </td>
                    </tr>
                    count++;
                }
            </tbody>
        </table> *@
    </div>
</div>

@await Html.PartialAsync("_ToasterNotification")
